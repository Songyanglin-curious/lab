# this 和 base

## 学习目标

- 理解 `this` 表示当前对象。
- 理解 `base` 用于访问基类成员。
- 理解构造函数调用链。

## 完成标准

- 能说明 `this` 在实例成员中的意义。
- 能说明 `base` 不是另一个对象。
- 能读懂构造函数中的 `this(...)` 和 `base(...)`。

## 沉淀文件

- `对话沉淀.md`
- `总结.md`
- `检查题.md`
