# nameof、typeof、sizeof、default

## 学习目标

- 理解这些关键表达式用于获取名称、类型、大小或默认值。
- 理解各自的使用场景和限制。

## 完成标准

- 能说明 `nameof` 的重构安全性。
- 能区分 `typeof` 和 `GetType()`。
- 能说明 `default` 对不同类型的结果。

## 沉淀文件

- `对话沉淀.md`
- `总结.md`
- `检查题.md`
