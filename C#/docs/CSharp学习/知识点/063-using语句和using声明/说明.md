# using 语句和 using 声明

## 学习目标

- 理解 `using` 语句和 `using` 声明用于确保资源释放。
- 区分资源释放用的 `using` 和命名空间导入用的 `using`。

## 完成标准

- 能写出两种资源释放用法。
- 能说明作用域结束时如何调用 `Dispose`。
- 能解释 `using` 不是异常处理的替代品。

## 沉淀文件

- `对话沉淀.md`
- `总结.md`
- `检查题.md`
