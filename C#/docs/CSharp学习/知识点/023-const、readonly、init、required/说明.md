# const、readonly、init、required

## 学习目标

- 理解常量、只读字段、初始化只读属性和必需成员。
- 理解它们在不可变对象建模中的作用。

## 完成标准

- 能区分 `const` 和 `readonly`。
- 能说明 `init` 和 `set` 的区别。
- 能说明 `required` 解决什么初始化问题。

## 沉淀文件

- `对话沉淀.md`
- `总结.md`
- `检查题.md`
